# Read about fixtures at http://ar.rubyonrails.org/classes/Fixtures.html

one:
  feed: service(Feed)
  field: one(Field)
  screen: one(Screen)
  weight: 1

two:
  feed: announcements(Feed)
  field: one(Field)
  screen: one(Screen)
  weight: 1

three:
  feed: service(Feed)
  field: one(Field)
  screen: two(Screen)
  weight: 1

four:
  feed: service2(Feed)
  field: one(Field)
  screen: one(Screen)
  weight: 2
